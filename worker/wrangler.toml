name = "leyfacil-groq-proxy"
main = "groq-proxy.js"
compatibility_date = "2024-01-01"

# Cuando tengas el dominio api.leyfacil.pro configurado en Cloudflare DNS, descomenta:
# routes = [{ pattern = "api.leyfacil.pro/*", zone_name = "leyfacil.pro" }]
